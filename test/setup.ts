/**
 * Global test setup
 *
 * This file runs once before all tests.
 * Use it for global configuration that applies to all tests.
 */

import { afterAll, beforeAll } from 'vitest';

beforeAll(() => {
    // Global setup code here
    // For example: initialize test database, set environment variables, etc.
});

afterAll(() => {
    // Global cleanup code here
    // For example: close connections, clean up resources, etc.
});
